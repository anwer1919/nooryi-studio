📜 ميثاق العمل — Nooryi Studio
🚫 القواعد الصارمة (غير قابلة للنقاش)
#
القاعدة
السبب
1
لا إعادة كتابة ملفات كاملة إلا إذا طلبت ذلك صراحةً
إعادة الكتابة تسبب فقدان تفاصيل دقيقة
2
لا تغيير مسارات الملفات أو المجلدات
يكسر الـ imports والروابط
3
لا تغيير أسماء المتغيرات أو الدوال
يكسر الاستدعاءات في ملفات أخرى
4
لا حذف أي ميزة موجودة
قد تعتمد عليها صفحات أخرى
5
لا تعديل schema.prisma إلا بموافقتك الصريحة
قد يكسر قاعدة البيانات
6
لا إضافة مكتبات جديدة إلا بطلب صريح
يزيد الحجم ويعقد الصيانة
7
الفحص قبل التعديل دائماً
التعديل الأعمى يسبب كوارث
8
التعديل الدقيق: استبدال أسطر محددة فقط
يحافظ على 99% من الكود الأصلي
✅ طريقة العمل المعتمدة (4 مراحل إلزامية)
المرحلة 1: الفحص 🔍
قبل أي تعديل، أطلب منك تنفيذ أمر فحص وقراءة الكود الفعلي:

12
المرحلة 2: التشخيص 🩺
أحدد المشكلة بدقة وأشرحها لك بجدول:

1
المرحلة 3: التنفيذ المضمون 🛠️
طريقة واحدة فقط معتمدة:
notepad اسم_السكريبت.py → يفتح Notepad
الصق الكود → احفظ بـ UTF-8
python اسم_السكريبت.py → تنفيذ
أمر واحد للـ git add + commit + push
ممنوع نهائياً:
❌ PowerShell هنا-سترينغز @"..."@ أو @'...'@
❌ أوامر Set-Content -Encoding UTF8
❌ أوامر طويلة قابلة للكسر بسبب الأقواس
المرحلة 4: التحقق ✅
أعطيك خطوات اختبار واضحة:

123
🎯 معايير الجودة للتعديل الدقيق
عندما أعدّل ملفاً موجوداً، أستخدم استبدال نصي دقيق مثل هذا:
✅ الطريقة الصحيحة (استبدال سطر محدد):
python

1
❌ الطريقة الخاطئة (إعادة كتابة الملف كامل):
python

1
📁 ملفات المشروع المحمية (لا تُعدّل أبداً)
الملف/المجلد
السبب
prisma/schema.prisma
بنية قاعدة البيانات
prisma/migrations/
تاريخ التغييرات
package.json / package-lock.json
التبعيات
next.config.ts
إعدادات البناء
src/lib/auth.ts
نظام المصادقة
src/lib/prisma.ts
اتصال قاعدة البيانات
.env / .env.local
الأسرار والبيانات الحساسة
